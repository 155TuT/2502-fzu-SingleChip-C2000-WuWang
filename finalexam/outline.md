# 单片机原理与应用复习提纲

## 嵌入式系统概述

1. 嵌入式系统的定义
2. 嵌入式系统和通用计算机系统的比较（共同点和不同点）、嵌入式系统的特点
3. 嵌入式系统的架构：冯･诺依曼结构和哈佛结构
4. 嵌入式系统处理器：MPU、MCU、DSP、SoC
5. MCU的基本组成：CPU、时钟、存储器、通用I/O、定时器/计数器、外设等组成。
6. CPU功能
7. 总线BUS：地址总线、数据总线（通常所说的8位、16位、32位MCU就是指CPU一次能够处理的数据位数，即数据总线的宽度）、控制总线

## C2000系列微控制器及硬件平台

1. F28027主要资源：32位CPU、时钟（40MHz/50MHz/60MHz）、哈佛总线架构、3.3V单电源供电、外设资源（GPIO、中断PIE、CPU定时器、ePWM、eCAP、ADC、SCI、SPI、I2C）
2. 引脚。功能复用、引脚有效符号，等
3. MCU硬件功能概述
4. 内存映射（数据空间/地址空间，空间大小与地址的转换
5. 复位向量地址（0x3FFFC0）
6. 时钟信号SYSCLKOUT
7. 看门狗电路（本质是定时器，“喂狗”：0x55AA）
8. 片内上电复位/欠电压复位

## 微控制器程序设计基础

1. 编程语言：机器语言、汇编语言、高级语言
2. CPU执行指令5个过程：取指令、指令译码、取操作数、执行指令、结果写回
3. object-C：几个特殊保留字（volatile、interrupt）

## 软件架构与CCS集成开发环境

1. 如何使用结构体指针操作寄存器
2. 寄存器位操作
3. 软件架构

## 通用输入输出口

1. GPIO基础知识
2. C2000的GPIO模块（概述、软件架构）
3. 软件思维导图：配置GPIO控制LED和按键

## 中断系统

1. 中断基础知识（中断概念、中断处理过程、中断嵌套）
2. C2000的中断系统（中断资源包含1个非可屏蔽中断NMI和16个可屏蔽中断（INT1-INT14、RTOSINT、DLOGINT）
3. PIE三级中断管理机制
4. 软件思维导图

## CPU定时器

1. 定时器基础知识（本质是计数器、计数方式、定时时间计算）
2. C2000的定时器（3个定时器、时钟基准SYSCLKOUT、减计数方式、预分频、周期、定时时间计算）
3. 软件思维导图

## 增强型捕获模块

1. 捕获模块基础知识（捕获MCU引脚的电平跳变（上升沿、下降沿），记录电平跳变的时刻）
2. 计数器工作模式：增计数
3. C2000的eCAP（概述、功能（预分频、边沿极性、连续/单次模式、事件决策、捕获寄存器、中断控制）
4. 软件思维导图

## 增强型脉宽调制模块

1. PWM基础知识（PWM概念、信号产生）
2. C2000的ePWM（TB子模块、CC子模块、AQ子模块、ET子模块）
3. 软件思维导图

## 模/数转换器

1. ADC基础知识
2. C2000的ADC（12位ADC内核、量程、SOC）
3. 软件思维导图

## 串行通信接口

1. 串行通信的基础知识
2. C2000的SCI（波特率、数据帧）
3. 软件思维导图

## 实验

1. 智能车与本课程F28027模块
2. 调试错误及处理
